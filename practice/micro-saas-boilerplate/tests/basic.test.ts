import { describe, it, expect } from '@jest/globals';

describe('Micro-SaaS Boilerplate', () => {
  describe('Basic Tests', () => {
    it('should pass basic test', () => {
      expect(true).toBe(true);
    });

    it('should add numbers correctly', () => {
      expect(1 + 1).toBe(2);
    });
  });
});
